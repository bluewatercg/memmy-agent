/**
 * Team sharing is not part of the current Memory Viewer release.
 * Keep the implementation available for a later release while hiding
 * every user-facing entry point for now.
 */
export const TEAM_SHARING_UI_ENABLED = false;
